# 結合試験チェックリスト: tohoku-series-2026-27

| 項目 | 内容 |
|---|---|
| タスクID | `tohoku-series-2026-27` |
| 作成日 | 2026-08-16 |
| 確認者 | Claude（ユーザー立会い・管理画面はユーザーのログインセッションを使用） |
| 確認日 | 2026-08-16 |
| 実施環境 | Docker（`cyclox2_svr` / `cyclox2_mysql`）、`feat/tohoku-series-2026-27` ブランチのコードを配信中 |
| DB保全 | 実施前に `cyclox2` 全体を `mysqldump`（383MB、スクラッチ領域に保存） |

> 単体テストの結果は `.kiro/specs/tohoku-series-2026-27/test-results.md` を参照。
> `ajocc-point-267-prod` の結合試験（17項目）は**未実施**。先行リリース前に別途実施が必要。

---

## 実施した試験のシナリオ

26-27シーズンの大会（`meets`）が0件のため、**既存のリザルトがある大会を使って付与経路を実際に通した**。
`TCX_267` は開催日を参照しない設計のため、この方法で配点ロジックを実データで検証できる。

| 項目 | 内容 |
|---|---|
| 検証用シリーズ | `point_series` id=457（東北グループ / 2026-27 / `calc_rule=TCX_267` / `sum_up_rule=全戦合計のみ` / `point_term_rule=無期限有効`） |
| 対象大会 | `TKI-256-006`（東海シクロクロス第6戦、2026-01-11、非JCX） |
| 対象カテゴリー | ME3（`entry_category_id=26660`、出走46名・45位まで記録、DNS1名） |
| 適用区分 | `started_over=39`（40人以上） |
| 実施後 | 検証用データを削除し再計算してベースラインへ復旧済み |

---

## 事前確認

- [x] 稼働中のアプリが実装済みコードを配信している（コンテナ内 `PointCalculator.php` に `TCX_267` を確認）
- [x] `cyclox2_svr` / `cyclox2_mysql` コンテナが起動している
- [x] `cyclox2ressys_svr`（成績閲覧アプリ）が起動している（`docker compose up -d` で起動、`:8081`）
- [x] 開発環境DBに 2026-27シーズン（`seasons.id=17`）が存在する
- [ ] **親リポジトリの submodule をリリース対象コミットへチェックアウトしている**
      → 本試験は `feat/tohoku-series-2026-27`（release 起点＋本改修）で実施。
      先行リリース直前に、release へのマージ結果で再確認すること

---

## 機能確認

### 管理画面での配点ルール選択（Requirement 1.6）

- [x] `PointSeries/add` の「配点ルール」選択肢に **`TCX_267`（value=14）** が表示される
- [x] 既存の選択肢（`val=1`〜`13`）が名称・値ともに変化していない（選択ずれなし）
- [x] `PointSeries/view`（シリーズ詳細）で配点ルールが `TCX_267` と表示される（fatal error なし）
- [x] `PointSeriesGroups/view`（グループ詳細）の配点Rule列で、新シリーズが `TCX_267`、
      既存25-26シリーズが `TCX_245` と表示される（fatal error なし）

### 説明テキストの表示崩れ（Requirement 1.5）

- [x] 配点ルール一覧ページ（`/pages/point_series_rules`）で、8区分の見出し
      （`100人以上` / `80人以上` / `60人以上` / `40人以上` / `20人以上` / `10人以上` /
      `5人以上` / `1人以上`）がすべて**独立した行**として描画される
- [x] 区分末尾と次区分の `---` が同一行に連結していない（`,---` の出現数 **0件**）

### シリーズ集計の動作（Requirement 1.3）

- [x] 検証用シリーズに大会を紐付け、対象カテゴリーのリザルト再計算を実行すると
      `point_series_racers` に行が作成される（**45行**）
- [x] 配点が新AJOCC得点表（40人以上区分）どおりである
      → **45件すべて期待値と一致（不一致0件）**。1位=350 / 10位=200 / 30位=50 / 45位=15
- [x] **順位が無い選手（DNS 1名）に行が作られない**（46リザルト中45行のみ作成）
- [x] シリーズランキングの集計（`calcup`）が完走し、「ランキングファイルを更新しました」となる

> 表範囲外の順位は、40人以上区分が59件の配点を持つのに対し記録順位が45位までのため発生しない。
> 各区分の配点件数は出走人数＋約20件が確保されており、実運用で表範囲外はほぼ発生しない。
> この経路は単体テスト（`testTcx267RankBoundariesPerDivision`）で全8区分について検証済み。

### CSV出力の形式非影響（Requirement 4）

- [x] シリーズランキングCSVが生成され、列構成が正常
      （`順位, 選手 Code, JCF-No., UCI-ID, 選手名, チーム名, #1検証, 合計`）
- [x] 文字コードが既存シリーズのCSV（`ranking_374.csv` = `JCX_245`）と同じ **UTF-8**
- [x] CSV内の配点値が 350 / 310 / 280 / 260 / 250 / 240 と40人以上区分どおり

---

## サービス間連携確認（res-sys）

第2ラウンドとして、公開設定つきの検証用シリーズ（`point_series` id=459 /
`publishes_on_ressys=1` / `publishes_newest_asap=1`）を作成し、同じ大会・カテゴリーに紐付けて
再計算 → 「公開データ作成」まで実施した。

- [x] 「公開データ作成」で `tmp_point_series_racer_sets` が生成される
      （ヘッダ1行＋45選手 = 46行、`public_psrset_group_id=1` が設定される）
- [x] res-sys のランキング一覧（`:8081/point_series`）に **「2026-27シーズン › 東北」** として
      新シリーズが表示される
- [x] res-sys のランキング詳細（`:8081/point_series/459`）が**表崩れなく描画**され、配点が
      350 / 310 / 280 / 260 / 250 / 240 / 230 / 220 / 210 / 200 …と40人以上区分どおりである
- [x] `publishes_on_ressys` の挙動が正しい
      （25-26東北シリーズは公開6本 ME1/WE1/MM1/CK1/CK2/CK3 のみ res-sys に表示され、
      非公開6本 ME2/ME3/ME4/WE2/MM2/MM3 は表示されない）
- [x] 検証用シリーズ削除後、res-sys の一覧から消え「2026-27シーズン」セクションも解消される

> res-sys 側のコードは `calc_rule` を一切参照せず（`grep calc_rule application/` → 0件）、
> `tmp_point_series_racer_sets` の確定済みポイントを読むだけである。したがって計算器の追加が
> res-sys の表示ロジックへ影響することは構造上ない。上記は実表示での裏取り。

---

## 非回帰確認

- [x] System①（`racer_results.ajocc_pt`）が本改修前と完全に同一
      （試験前後とも 46行・合計 **1839**。1位=180pt で25-26の旧3区分表のまま）
- [x] 既存シリーズ（`TCX_245`=12 等）の表示・配点ルール名が変化していない
- [x] シリーズ詳細・グループ詳細画面が既存の全シリーズで fatal error にならない

---

## 復旧確認

- [x] 検証用 `meet_point_series`（id=4045 / 4046）を削除
- [x] 検証用 `point_series`（id=457 / 459）を削除
- [x] 再計算を実行し、`point_series_racers` が **0件**（ベースラインと一致）に戻った
- [x] `ajocc_pt` 合計が **1839**（ベースラインと一致）で不変
- [x] res-sys の一覧から検証用シリーズが消え、2026-27セクションも解消された

> 検証用の4レコードはアプリの削除機能により**論理削除**（`deleted=1`）されている。
> 一覧画面には表示されない。物理削除が必要な場合は別途実施すること。
> `tmp_point_series_racer_sets` の公開データ（`point_series_id=459` の46行）も残存するが、
> 親シリーズが削除済みのため res-sys からは参照されない。

---

## 未実施の項目（先行リリース前に必要）

- [x] `ajocc-point-267-prod` の `integration-test-checklist.md` を合同で実施
      （2026-08-16。詳細は同ファイルを参照。境界日付テストは既存大会の `at_date` を一時変更する
      方式で実施し、確認後に原状復帰済み）
- [ ] 先行リリース直前に、submodule をリリース対象コミット（release へのマージ結果）へ
      チェックアウトして主要項目を再確認
- [ ] 先行リリースのマージ方式が `--no-ff` であることの確認
- [ ] ロールバック手順（`calc_rule=14` の `point_series` は**物理削除**が必要。論理削除では
      `__resetSeriesPoints()` を防げない）を関係者に共有
