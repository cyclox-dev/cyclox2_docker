# Implementation Plan

- [ ] 1. リアルタイム昇格枠の変更（MM2→MM1 上限3名、TDD）
- [ ] 1.1 2026-27昇格枠のルール値を確定し、シーズン境界の昇格枠テストを先に書く
  - AJOCC 公式改正文（2026-27 amendment）と照合し、MM2→MM1 の出走人数区分別の昇格枠
    （上限3名は確定。人数閾値の刻み）を確定して agreement-log.md に追記する
  - 確定値に基づき、失敗するテストを先に書く: 2026-04-01 以降のレースで CM2→CM1 昇格が
    最上位人数区分で 3 位まで適用され 4 位は適用されないこと、2026-03-31 以前のレースでは
    上限 2 名のままであること（境界日の前日・当日・翌日）、CM2+3 レースが CM2 と同一ルールで
    判定されること
  - テストファイル・Fixture は me-mm-linkage-2026-27 が先に整備していれば追記、なければ
    新規作成する（どちらでも成立する構成にする）
  - Observable: 新規テストが現行実装（上限2名）に対して失敗し、期待値（26-27 は上限3名・
    過去シーズンは2名）を表現していることをテスト実行結果で確認できる
  - _Requirements: 1.1, 1.2, 1.4_
  - _Boundary: ResultParamCalcComponent_

- [ ] 1.2 新ルール配列・シーズン判定・2026-27分岐を実装しテストを通す
  - 新ルール配列を既存のルール配列宣言部に追加し、有効シーズン（26-27〜）と出典を
    コメントで記録する
  - 2026-27 シーズン判定メソッド（大会開催日 > 2026-03-31）を既存の判定メソッド群の
    規約に従って追加する
  - 昇格ルール構築処理の最上位に 2026-27 分岐を追加する。分岐内容は 24-25 分岐の複製とし、
    CM2 / CM2+3 のルールのみ新配列へ差し替える（既存行は一切変更しない）
  - Observable: 1.1 のテストが全て成功し、既存のテスト（他カテゴリーの昇格枠・過去シーズン
    の判定）にも回帰がないことをテスト実行結果で確認できる
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 6.1, 6.3, 6.4_
  - _Boundary: ResultParamCalcComponent_

- [ ] 2. シーズン末判定SQL資産の作成
- [ ] 2.1 (P) 判定パラメータ宣言部と自基準充足判定（第1段 survive_flags）を作成する
  - 判定セット生成 SQL の冒頭に全ルール値を SET 変数として集約する: season_id・今季昇格
    判定窓・ME 順位ライン（C1=240 / C2=240 / C3=240。C2/C3 が新値）・MM 点数ライン
    （CM1=80 / CM2=40。変更なし）・WE1 順位ライン（80。新設）。各変数に有効シーズンと
    出典（AJOCC 改正文）のコメントを付す
  - racer×category（C1, C2, C3, CM1, CM2, CL1）ごとの自基準充足フラグと今季昇格者フラグを
    持つ中間表 survive_flags を構築する。今季昇格者は充足扱いとする
  - WE1 の順位判定は全国版 AJOCC ランキング（CL1）を参照し、出走照合用の参考クエリ
    （races_category_code IN ('CL1','UCIWE')、status<>0）を確認出力として同梱する
  - Observable: ローカル DB で SQL を実行すると survive_flags が生成され、カテゴリー別の
    充足/不充足/今季昇格の件数が確認出力に表示される
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 5.4, 6.2, 6.3, 6.4, 8.1, 8.2_
  - _Boundary: 判定セット生成SQL_

- [ ] 2.2 系統横断残留・ME1例外・連動降格を適用した降格セット（第2段 demote_all）を作成する
  - survive_flags と有効保有情報を結合し、救済規則を適用して demote_all を導出する:
    C2⇔CM1・C3⇔CM2 はペアの一方充足で両残留／両不充足で両降格、C1（ME1）は自基準のみで
    判定（MM1 による救済なし）、CM1 は C2 充足または C1 充足（元ME1ペア）で救済、
    単独保有は自基準のみ、対応外ペアは救済なしの個別判定
  - 対応ペア定義・降格先マッピングには me-mm-linkage-2026-27 の CategoryLineageMap を
    出典とする複製である旨のコメントを必ず付す
  - 確認出力を追加する: カテゴリー別降格件数、系統横断救済の適用者数、連動降格ペア数、
    対応外ペア保有者数
  - demote_all のスキーマは 2025-26 版（racer_code, src, ord_rank, ord_pt）と互換にする
  - Observable: ローカル DB で SQL を実行すると demote_all が生成され、「自基準不充足だが
    相手側充足」の選手が demote_all に含まれず、救済適用者数が確認出力に表示される
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 4.1, 4.2, 4.3, 5.1, 5.2, 5.3, 5.4, 7.1, 7.3_
  - _Boundary: 判定セット生成SQL_
  - _Depends: 2.1_

- [ ] 2.3 降格SQL生成スクリプトを2026-27版に改訂する
  - 2025-26 版の生成スクリプトをベースに、年次パラメータ（apply=2027-04-01 /
    cancel=2027-03-31 / reason_note=2026-27シーズン成績の降格処理による）を更新する
  - 降格先マッピング（C1→C2, C2→C3, C3→C4, CM1→CM2, CM2→CM3, CL1→CL2）に対応表出典
    コメントを付す。連動降格は demote_all のペア2行から自然に両系統の SQL が生成される
    構造とし、スクリプト自体にペア認識ロジックは持たせない
  - MYSQL_PWD 未設定時に失敗する既存ガード（秘密情報ハードコード禁止）と、出力先が
    git 管理外である旨の注記を維持する
  - Observable: demote_all を入力に downlist（カテゴリー別）と降格 SQL（INSERT+UPDATE）が
    生成され、連動降格対象の選手について両系統分の SQL が含まれる
  - _Requirements: 5.1, 5.3, 7.1_
  - _Boundary: 降格SQL生成スクリプト_
  - _Depends: 2.2_

- [ ] 2.4 検証SQLを連動降格対応の新基準に改訂する
  - 「二重降格」を再定義する: 同一系統内の複数降格 = 0 期待（異常）、対応外ペアの同時降格
    = 0 期待（異常）、対応ペアの連動降格 = 正常系として件数出力
  - 継承検証（降格先別件数・旧カテゴリーアクティブ行残存 = 0）に加え、連動降格選手の
    降格先ペアが対応表上の正当なペアであること（違反 0 期待）を検証する
  - category_races_categories から CL1 の集計対象 race_category 一覧を出力し、UCIWE の
    包含を目視確認できるようにする
  - Observable: 降格 SQL 適用後（TRANSACTION 内）に検証 SQL を実行すると、異常系 0 件・
    連動降格件数・WE1 集計対象一覧が出力される
  - _Requirements: 7.3, 7.4, 8.3_
  - _Boundary: 検証SQL_
  - _Depends: 2.3_

- [ ] 3. runbook 2026-27版の作成
  - 2025-26 版 runbook の構成を踏襲し改訂する: §2 パラメータ表（season_id は実行時に
    seasons テーブルで確認・残留ライン新値と有効シーズン・出典）、§3 判定基準への
    系統横断残留・ME1例外・連動降格の追加、§4 二重降格の再定義、§6 シミュレーション時
    ROLLBACK／本番時のみ COMMIT の分岐、§8 WE1 80位境界の照合追加
  - 「次年度の更新点」に対応表変更時の SQL 追随チェック（me-mm-linkage の CategoryLineageMap
    変更を Revalidation Trigger とする）を追加する
  - 本番実行（2027-03）の判断・COMMIT は人間のみが行う旨、過去シーズンの判定資産
    （.kiro/specs/rider-demotion-2025-26/）は変更しない旨を明記する
  - downlist・降格 SQL・実行ログの出力先を git 管理外とする PII 取り扱い注意を明記する
  - Observable: runbook.md のみを参照して、ダンプ復元→判定→生成→シミュレーション→検証→
    ROLLBACK の一連の手順を人間が実行できる記載になっている
  - _Requirements: 2.6, 6.4, 7.1, 7.2, 7.4, 7.5, 8.3_
  - _Boundary: runbook_
  - _Depends: 2.4_

- [ ] 4. ローカルダンプ検証
- [ ] 4.1 2025-26データダンプで新判定ロジックのシミュレーションを実行し記録する
  - runbook の手順に従い、ローカル cyclox2_mysql コンテナへ 2025-26 ダンプを復元し
    （季節パラメータは season_id=16 に読み替え）、判定 SQL →生成スクリプト→降格 SQL の
    TRANSACTION 内適用→検証 SQL →ROLLBACK を通しで実行する
  - カテゴリー別件数を旧ルール実績（93/107/202/48/113/20=583）と比較し、差分を要因別
    （ME2/ME3 ライン変更・WE1 基準変更・系統横断救済・連動降格）に説明する
  - ME1 例外（C1 不充足×CM1 充足の選手の扱い）・救済適用者・連動降格ペア・対応外ペア
    保有者数を確認し、本番 DB に一切変更を加えていないことを確認する
  - 実行コマンド・出力・差分分析を test-results.md に記録する
  - Observable: test-results.md にシミュレーション結果（件数・差分説明・ROLLBACK 完了）が
    記録され、ローカル DB の category_racers が実行前と同一件数である
  - _Requirements: 7.2, 7.3, 7.4, 8.1, 8.2_
  - _Depends: 3_

- [ ] 4.2 境界ケースの照合と結合試験チェックリストを作成する
  - 公開ランキングと境界選手を1件ずつ突合する: ME2/ME3 の 240 位境界（240=残留、241=降格）、
    WE1 の 80 位境界、点数境界（CM1=80pt / CM2=40pt）、☆今季昇格者の除外
  - WE1 について UCIWE のみ出走の選手が出走照合クエリで計上されることを確認する
    （2025-26 の取りこぼし事例の再発防止）
  - 系統横断救済が適用された選手のサンプルを抽出し、両系統のランキング・所属と突合する
  - 照合結果と 2027-03 本番反映前チェック項目（バックアップ取得・件数再確認・ressys 表示
    確認等）を integration-test-checklist.md に記録する
  - Observable: integration-test-checklist.md に境界照合の結果（各境界で一致）と本番前
    チェック項目が記録されている
  - _Requirements: 7.3, 8.1, 8.2, 8.3_
  - _Depends: 4.1_
