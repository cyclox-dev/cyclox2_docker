cyclox2_mysql/dump/*
cyclox2_mysql/mysql/*
phpmyadmin/*
cyclox2_mysql/var/
tmp/

# シミュレーション生成物（個人情報含む・再生成可能なため履歴に残さない）
# 【レガシー】.kiro/specs/ 統一（2026-07〜）前の既存specのみ対象。新規タスクは下記 .kiro/specs/*/outputs/ を使う。
docs/specs/*/outputs/

# シークレット（実値はコミットしない。*.example を雛形として使用）
.env
cyclox2_svr/cyclox2_conf/database.php
# --- SDD base (sdd_base_template) ---
# タスクごとの生成物（個人情報を含み得る・再生成可能なため履歴に残さない）
.kiro/specs/*/outputs/
# init の上書き/対比モードで作るバックアップ
.sdd-backup/
# sync実行のたびに上書きされるレポート（lock/snapshotは対象外＝コミットする）
.kiro/sdd-base-update-report.md
# 大容量・一時ファイル
# sync 実行のたびに上書きされるレポート（実行時ログ・履歴として残さない）
