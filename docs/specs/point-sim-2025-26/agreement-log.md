# 合意形成記録: 2025-26シーズン 新ポイントテーブル ランキングシミュレーション

| 項目 | 内容 |
|---|---|
| タスクID | `point-sim-2025-26` |
| 作成日 | 2026-06-14 |
| 関係者 | KYamada（AJOCC） / Claude Code |

---

## 壁打ち概要

新しいAJOCC得点表（スプレッドシート「AJOCC得点表 2026-2027」）を 2025-26 シーズンの全レースに適用してポイントを振り直した場合、ランキングがどう変わるかを開発環境でシミュレーションする。本番データ・本番環境には一切影響を与えない。

調査の結果、ポイントは**2系統が独立**して存在することが判明した:
- **System①: AJOCCポイント (`ajocc_pt`)** … `ResultParamCalcComponent::__getAjoccPointMap()`。カテゴリ別「AJOCCランキング」の素点。
- **System②: ポイントシリーズ点** … `PointCalculator::getCalculator(calc_rule)`。シリーズ別「ポイントシリーズランキング」の素点。2025-26のJCF/JCXシリーズは `calc_rule=11`（JCX_245）を使用。

---

## 決定事項

| # | 決定内容 | 決定理由 | 決定日 |
|---|---|---|---|
| 1 | CSV出力は org_util の既存2機能（AJOCCランキング `download_ajocc_pt_csv` / ポイントシリーズランキング `point_series_csv_links`）と同形式とする | 既存運用と同じ形式で比較したい | 2026-06-14 |
| 2 | ポイントシリーズランキングの対象は 2025-26 の **JCFシリーズ・JCXシリーズ**（`calc_rule=11`） | 注目シリーズに限定 | 2026-06-14 |
| 3 | 成果は**開発ブランチとして残す**。DBは戻さない（`tmp/`配下にダンプ確保済み） | 再現・追検証のため | 2026-06-14 |
| 4 | 新ポイントテーブル名は **`AJOCC_267_TEST`** | 命名規則（24-25=245 → 26-27=267）に準拠＋テスト識別 | 2026-06-14 |
| 5 | System①（AJOCCポイント）: 非JCX大会は新8区分表、**JCX大会(`is_jcx=1`)は新JCX列**（1000,900,810…）を適用 | スプレッドシートのJCX列をJCX大会のAJOCC点に適用 | 2026-06-14 |
| 6 | System②（JCX_245/`calc_rule=11`）: スプレッドシート右端の**JCX列**を新シリーズ表として適用。**GRADE1/GRADE2の区別を廃し、両グレードとも新JCX列**を使用 | JCX戦の新テーブルでのランキングが取得できればよい | 2026-06-14 |
| 7 | JCX列は現行JCX_245（GRADE1=400,320… / GRADE2=200,160…）と**差分あり**を確認済み → シリーズ表の改修が必要 | 数値比較で不一致を確認 | 2026-06-14 |
| 8 | **25-26テーブル（別ファイル: `1T350wA8...`）は現行本番コードと完全一致**のため、25-26用の作業は**一切不要**。26-27テーブルの作業のみ実施 | 25-26は現行ルールそのもの＝現行ランキングを再現するため | 2026-06-14 |
| 9 | 26-27適用で2025-26のDB値が上書きされるため、**変更前に現行（=25-26ベースライン）のランキングCSVを退避**してから26-27作業を行う | 26-27 vs 現行 の比較を可能にするため | 2026-06-14 |
| 10 | **全日本選手権(GRADE1)も新JCX列(1位=1000)を適用**（現行400から上昇）してよい | グレード区別を廃する方針(#6)の確認。人間が明示承認（「全日本もそれでよい」）| 2026-06-14 |
| 11 | ランキング生成CSV（個人情報含む）は**git管理外**（`docs/specs/*/outputs/` を`.gitignore`）。再生成可能なためディスク保持のみ | PIIをgit履歴に残さない。人間が選択（選択肢A）| 2026-06-15 |
| 12 | 降格ライン分析の基準は **AJOCC公式規定**（cyclocross.jp）に準拠。マスターズ=点数(MM1:80/MM2:40)、エリート=順位(ME1:240/ME2:260/ME3:280)、WE1=出走経験 | 人間が公式URLを提示 | 2026-06-15 |
| 13 | クライアント提出物として**PPT分析資料**を作成（前後比較・順位変動傾向・降格ライン分析・懸念） | 人間の依頼。Driveアップロードは人間側で対応 | 2026-06-15 |
| 14 | 実装は**アプリ本体submodule(cyclox2web)とdockerリポの2つ**に分け、いずれも作業ブランチ→PRで反映（main直コミット禁止を遵守） | ブランチ・PRポリシー | 2026-06-15 |
| 15 | PR #12 の日付分岐は**シミュレーション専用フラグ(`__simAjocc267`)でガード**し、本番デフォルト無効とする。267_TEST は他年度テーブル同様の配置のまま残し、**本番採用時はフラグ条件解除＋リネームのみ**で対応する | 初版は日付のみで本番 `reCalcResults` に 267_TEST が漏れ、マージで本番ポイントが上書きされる懸念。要件 R6 を追加し隔離。将来本番化を見据え配置は不変 | 2026-06-20 |

---

## 確認済みの差分（決定#7の根拠）

| 順位 | JCX列(新) | 現行G1(全日本) | 現行G2(JCX戦) |
|---|---|---|---|
| 1 | 1000 | 400 | 200 |
| 2 | 900 | 320 | 160 |
| 3 | 810 | 280 | 140 |
| 4 | 730 | 240 | 120 |
| 5 | 660 | 220 | 110 |

→ 両グレードとも不一致のため改修必要。

---

## 対象規模（2025-26 / season_id=16）※実測で確定

| 対象 | 件数 |
|---|---|
| 大会数 | 66 |
| 出走カテゴリー総数 | 1,192 |
| **再計算対象（リザルト有・実処理）** | **1,170** |
| `is_jcx=1` 大会 | 15 |
| `calc_rule=11`→`13` 付替えシリーズ（JCF/JCX/関西） | **23**（当初35と記載したが実測23に訂正） |
| AJOCCランキング出力カテゴリ（データ有） | 29 |

---

## フェーズゲート承認記録（実績）

| フェーズ | 承認日 | 承認者 | 備考 |
|---|---|---|---|
| 要件定義（requirements.md） | 2026-06-14 | KYamada | 壁打ち（AskUserQuestion）で要件確定 |
| 設計（design.md） | 2026-06-14 | KYamada | 「設計方針はこれで承認する」。追加で25-26テーブルの確認を実施 |
| タスク分解・実装前確認（tasks.md） | 2026-06-14 | KYamada | 「すすめて」で実装着手を承認 |
| 実装・再計算・出力 | 2026-06-15 | KYamada | 「再開」「進めて」で逐次承認。完了 |
| コミット・push・PR | 2026-06-15 | KYamada | 「この方針でコミット」「両方OK」で承認・実施 |
| 追補: 本番隔離フラグ（R6/変更5） | 2026-06-20 | KYamada | PR #12 レビューで本番漏れを指摘・フラグ方式で改修を承認。「フラグ方式で問題ない」 |

---

## 人間の関与・壁打ち結果（実績サマリ）

- スコープ・言語・枠組み・配布方式は AskUserQuestion で逐次合意（cc-sdd採用、日本語、submodule含む把握）。
- 「降格ラインに絡む順位変動」分析の追加を人間が依頼。残留基準は公式規定（cyclocross.jp）を人間が提示。
- 出走人数の解釈（カテゴリ単位）・全日本のグレード廃止は人間が明示承認。
- バイパス権限モードを人間操作で有効化（worktree settings.local.json に defaultMode 設定）。

---

## 実装中に発生した重要事象（人間に報告・対応）

| 事象 | 影響 | 対応 |
|---|---|---|
| バッチ全件スキップ | 再計算0件 | クエリ結果キー誤り(`$r[0]`→`$r['ec']`)を修正 |
| Flash null で停止 | 200件で中断 | リザルト無カテゴリを除外＋try/catch |
| **出走人数 `__started` の累積バグ** | ほぼ全カテゴリが最上位tier(400)＝誤結果 | **カテゴリ毎に新インスタンス生成**（本番と同挙動）で修正・再実行 |
| `racer_results` の重複行 | 検証時に「rank1複数」に見えた | `deleted=0`(論理削除)で有効行が一意と判明。本番由来でDB破損ではない |
| LibreOfficeのPDF変換クラッシュ | PPTのQA不可 | 影付き図形を全廃（境界線デザイン）で解消 |

---

## 成果物

- アプリ実装（submodule cyclox2web, ブランチ `feat/point-table-ajocc-267-sim`, PR #12）
- SDDドキュメント（dockerリポ, ブランチ `claude/romantic-lalande-4bb2be`, PR #8）
- ランキングCSV（現行/26-27 × AJOCC29・シリーズ23）: `outputs/`（git管理外）
- クライアント提出用PPT: `outputs/ranking_simulation_analysis.pptx`（git管理外）
- 比較サマリー: `comparison-summary.md`（降格ライン分析含む）

---

## 変更履歴

| 日付 | 変更内容 | 変更者 |
|---|---|---|
| 2026-06-14 | 初版作成（調査・壁打ち結果を記録） | Claude Code |
| 2026-06-15 | 実績反映（承認記録・決定#10〜14・対象規模実測訂正・重要事象・成果物） | Claude Code |
| 2026-06-20 | 本番隔離フラグ追補（要件R6・設計変更5・決定#15・承認記録・検証結果） | Claude Code |
