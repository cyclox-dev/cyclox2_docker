# 結合試験チェックリスト: 2025-26 新ポイントテーブル ランキングシミュレーション

| 項目 | 内容 |
|---|---|
| タスクID | `point-sim-2025-26` |
| 作成日 | 2026-06-15 |
| 確認者 | （人間：業務妥当性の最終確認） |
| 確認日 | |

> 人間が手動で確認すべき項目。シミュレーション結果の妥当性を業務観点で検証する。
>
> **自動検証済み（Claude, 2026-06-15）**：出力CSVの妥当性（JCX全日本 400→1000 / AJOCC tier分布 / 同形式）、
> 計算ロジック（単体32/32 PASS, 単一カテゴリ手計算一致）、他シーズン不変（2024-25回帰）は確認済み。
> **未確認（要・競技運営者の判断）**：下記「業務妥当性」セクションの各項目（新表の意図整合、降格閾値の扱い等）。

---

## 事前確認
- [ ] Dockerコンテナ稼働 (`docker-compose ps`)
- [ ] 開発環境のDBであること（本番でないこと）を確認

---

## 出力CSVの妥当性
- [ ] `outputs/after_2627/series/` のJCX男子エリートで、全日本(二色の浜)の配点が 400→1000 になっている
- [ ] `outputs/after_2627/ajocc/` の代表カテゴリで、大規模フィールド優勝者の点が上昇している
- [ ] 現行(baseline)と26-27(after)が選手コードで突合でき、同一フォーマットである
- [ ] 既存のWeb UI（org_util）からダウンロードできるCSVと列構成が一致している

## 業務妥当性（要・競技運営者の判断）
- [ ] AJOCCランキングで上位・1位まで大きく変動するのは新表の意図と整合するか
- [ ] 「出走人数」をカテゴリ単位で数える解釈で正しいか（合同出走でもカテゴリ別）
- [ ] JCX/JCFシリーズで全グレード同一(JCX列)・グレード区別廃止で問題ないか
- [ ] 25-26テーブルは現行と一致＝シミュレーション不要だった点に異論がないか

## 計算ロジックのスポット確認
- [ ] 任意の1カテゴリを手計算し、新表での順位・点と一致するか
- [ ] DNS/DNF/OPEN参加者の扱いが現行と同じか（ポイント対象外）
- [ ] 2024-25など他シーズンのランキングが変化していないか（Web UIで確認）

## 本番フロー隔離フラグ（R6 / 変更5）※2026-06-20 追加
- [ ] `Console/cake PointSim testTables` を Docker 上で再実行し、「本番OFF=180/200・シムON=350/1000」の対比アサーションが PASS する
- [ ] **Web の結果入力・編集で 2025-26 大会のリザルトを再計算しても `ajocc_pt` が 267_TEST にならない**（従来表のまま）ことを確認（フラグ未使用フロー）
- [ ] シミュレーション再計算（`PointSim recalcSeason 16`）では 267_TEST が適用される（フラグ有効）ことを確認

## 後始末・方針
- [ ] 開発ブランチ `feat/point-table-ajocc-267-sim`（cyclox2web submodule, PR #12）を残す方針で合意
- [ ] DBを戻す場合は `tmp/` のダンプから再リストア（戻さない方針なら不要）

---

## 確認結果
| 区分 | 件数 |
|---|---|
| 確認済み | / 全項目 |
| NG | |

### NGメモ
| # | 項目 | 内容 | 対応 |
|---|---|---|---|
| | | | |
